<% @registered_applications.each do |registered_application| %>
  <ul>
  <%= registered_application.name %>
  <%= registered_application.url %>
  <%= link_to 'View', registered_application %>
  </ul>
<% end %> <br>

<%= link_to "New App", new_registered_application_path %>
